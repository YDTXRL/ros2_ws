常用技巧：只 build 某一个包
colcon build --packages-select my_py_pkg


#git checkout -b 分支名
#git checkout 分支名


git switch -c 分支名
git switch 分支名
git branch
git branch -d 分支名
git merge 分支名

git push
git pull
git add .
git commit -m "说明"
